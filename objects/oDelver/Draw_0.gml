/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();


if (global.DEBUG && visualise_circle)
{
	draw_circle(x + dir * attack_x_shift * scale, y, attack_radius * scale, false);
}

